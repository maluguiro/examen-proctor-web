"use client";

export default function HomeActionButton() {
  return (
    <button
      type="button"
      onClick={() => alert("Funcionalidad de alumno próximamente...")}
      className="<!-- PEGAR AQUÍ el className exacto que tiene hoy el botón -->"
    >
      Ingresar como Alumno
    </button>
  );
}
